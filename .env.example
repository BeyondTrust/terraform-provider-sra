# Example .env file for local test runs.
# Copy this to .env (which is git-ignored) and fill in real credentials.

BT_API_HOST=https://api.example.com
BT_CLIENT_ID=your-client-id
BT_CLIENT_SECRET=your-client-secret
